"""
Auth module — Pydantic schemas for request/response validation.

TODO: Implement the following schemas:
    - UserRegisterRequest (email, password, first_name, last_name, role)
    - UserLoginRequest (email, password)
    - UserResponse (id, email, first_name, last_name, role, is_active)
    - TokenResponse (access_token, token_type)
"""
