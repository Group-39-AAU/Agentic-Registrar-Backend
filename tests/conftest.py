"""
Shared test fixtures and configuration — placeholder.

TODO: Implement:
    - Test database engine (async SQLite in-memory)
    - TestSessionLocal factory
    - setup_database fixture (create/drop tables per test)
    - override_get_db (swap real DB for test DB)
    - client fixture (httpx AsyncClient with dependency overrides)
"""
